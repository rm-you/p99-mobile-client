[package]
name = "tauri-plugin-session-service"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-session-service"
license = "MIT"
publish = false

[dependencies]
tauri = { version = "2" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
