<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application>
        <provider android:name="io.github.rmyou.sessionservice.ChatExportProvider" android:authorities="${applicationId}.chat-exports" android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/chat_export_paths" />
        </provider>
        <activity
            android:name="${applicationId}.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/P99ChatTheme" />
        <service
            android:name="io.github.rmyou.sessionservice.ChatConnectionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started live game chat session over a persistent legacy UDP connection. The game servers do not support push notifications. Receive chat and maintain protocol heartbeats until the user disconnects; an ongoing notification provides a Stop action." />
        </service>
    </application>
</manifest>
